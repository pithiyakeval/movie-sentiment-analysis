name: Movie Sentiment API CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        cd flask-app
        pip install -r requirements.txt
    
    - name: Run tests
      run: |
        cd flask-app
        python -c "import nltk; nltk.download('vader_lexicon'); nltk.download('punkt')"
        # Add your test commands here
        echo "Tests would run here"

  build:
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Build Docker image
      run: |
        docker build -t movie-sentiment-api:latest .
    
    - name: Test Docker image
      run: |
        docker run --rm movie-sentiment-api:latest python -c "
        import nltk
        nltk.data.path.append('/usr/local/nltk_data')
        try:
            nltk.data.find('sentiment/vader_lexicon.zip')
            print('✅ NLTK VADER working in Docker')
        except:
            print('❌ NLTK VADER not working')
        "

  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Deploy to Docker Hub (Optional)
      run: |
        echo "This would push to Docker Hub in real scenario"
        # docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
        # docker tag movie-sentiment-api:latest ${{ secrets.DOCKER_USERNAME }}/movie-sentiment-api:latest
        # docker push ${{ secrets.DOCKER_USERNAME }}/movie-sentiment-api:latest